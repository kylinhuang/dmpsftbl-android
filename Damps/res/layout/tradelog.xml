<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <RelativeLayout
        android:id="@+id/rl_tradelog_layout1"
        style="@style/titlebar" >

        <ImageView
            android:id="@+id/iv_tradelog_back"
            style="@style/pic_title_left" />

        <TextView
            android:id="@+id/tv_tradelog_title"
            style="@style/title"
            android:layout_toRightOf="@+id/iv_tradelog_back"
            android:text="@string/tradelog" />

        <ProgressBar
            android:id="@+id/pb_tradelog_bar1"
            style="@style/progressbar" />

    </RelativeLayout>

    <ListView
        android:id="@+id/android:list"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/rl_tradelog_layout1" >
    </ListView>

</RelativeLayout>